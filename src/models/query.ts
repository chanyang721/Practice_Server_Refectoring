import { Request, Response, NextFunction } from "express";
import db from "../database";
import { Iinstructor, Ilecture, Istudent } from "../interfaces"


export const createStudentQuery = (req: Request, res: Response, next: NextFunction) => {

}